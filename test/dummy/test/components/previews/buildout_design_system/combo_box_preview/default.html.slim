= render(BuildoutDesignSystem::ComboBox.new) do
  label.form-label Hello World
  = select_tag "hello_world", options_for_select([%w[First first], %w[Second second], %w[Third third], %w[Fourth fourth]]), prompt: with_blank ? "Please select an option" : nil
