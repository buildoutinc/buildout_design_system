{
  "name": "buildout-design-system",
  "description": "A Collection of reusable components for the Buildout Design System",
  "version": "0.0.1",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "css:watch": "sass --watch --update --style=expanded lib/assets/stylesheets:vendor/assets/stylesheets",
    "css:build": "sass --no-source-map --update --style=compressed lib/assets/stylesheets:vendor/assets/stylesheets"
  },
  "author": "Joel Lopez",
  "devDependencies": {
    "sass": "^1.69.5"
  }
}